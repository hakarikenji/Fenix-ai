# Fenix AI — production runtime
flask>=3.0.0
gunicorn>=21.2.0
google-genai>=1.0.0
python-dotenv>=1.0.0
Pillow>=10.0.0
# Optional error monitoring (enabled only when SENTRY_DSN is set)
sentry-sdk[flask]>=2.0.0

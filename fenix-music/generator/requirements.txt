# Fenix Music audio worker dependencies.
# The worker pins its own image (see worker.py); this file exists so the same
# set can be installed locally or on any other host.
#
# Deliberately minimal: the model ships its own transformers implementation, so
# audiocraft / ffmpeg / xformers are not needed and are the usual cause of
# install failures.
torch>=2.3
transformers>=4.51
numpy
fastapi
